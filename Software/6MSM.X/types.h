/* 
 * File:   types.h
 * Author: User
 *
 * Created on September 29, 2013, 11:28 PM
 */

#ifndef TYPES_H
#define	TYPES_H

#define BOOL unsigned char
#define TRUE (1==1)
#define FALSE (!TRUE)

#define NULL	0

#define MAX(x,y) ((x) > (y) ? (x) : (y))
#define MIN(x,y) ((x) < (y) ? (x) : (y))

#endif	/* TYPES_H */

